<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>
    <div class="buttons">
      <a href="/chicken">
        <button type="submit" name="choice">🍗</button>
      </a>

      <a href="/beef">
        <button type="submit" value="beef" name="choice">🥩</button>
      </a>

      <a href="/fish">
        <button type="submit" value="fish" name="choice">🐟</button>
      </a>
    </div>

    <div id="recipeContainer" class="hidden">
      <!-- If no recipe yet, show: -->
      <% if (!recipe) {%>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } else {%>
      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->

      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= recipe.ingredients.protein.name %>, <%=
          recipe.ingredients.protein.preparation %>
        </li>
        <li><%= recipe.ingredients.salsa.name %></li>
        <!-- <li><% for(i = 0; i < recipe.ingredients.toppings.length; i++) {%>
        <li><%= recipe.ingredients.toppings[i].quantity%> of <%= recipe.ingredients.toppings[i].name%> </li>

      <% } %></li> -->
        <% recipe.ingredients.toppings.forEach(topping => { %>
        <li><%= topping.quantity %> of <%= topping.name %></li>
        <% }) %>
      </ul>

      <% }%>
    </div>
  </body>
</html>
